skip_local_validator = true

[toolchain]

[features]
resolution = true
skip-lint = false

[programs.localnet]
perp = "FWmruxC6TfBGZyXbQtzNjjJVrYMzRWLsTr6iscs9bkyK"

[provider]
cluster = "localnet"
wallet = "~/.config/solana/id.json"

[scripts]
# For unit tests just use `cargo test`
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"

[hooks]
